"""Bài luyện tập Recursion (đệ quy).

Yêu cầu: cài đặt TẤT CẢ các hàm dưới đây BẰNG ĐỆ QUY (không dùng vòng lặp).
Hiện tại các hàm chỉ là stub, hãy thay thân hàm để vượt qua test trong
test_recursion.py.
"""


def factorial(n: int) -> int:
    """Tính n! bằng đệ quy. factorial(0) = 1. Báo ValueError nếu n < 0."""
    raise NotImplementedError("TODO: cài factorial bằng đệ quy")


def fib(n: int) -> int:
    """Tính số Fibonacci thứ n bằng đệ quy. fib(0)=0, fib(1)=1. ValueError nếu n < 0."""
    raise NotImplementedError("TODO: cài fib bằng đệ quy")


def sum_to_n(n: int) -> int:
    """Tính tổng 1 + 2 + ... + n bằng đệ quy. sum_to_n(0) = 0."""
    raise NotImplementedError("TODO: cài sum_to_n bằng đệ quy")


def power(base: int, exp: int) -> int:
    """Tính base^exp (exp >= 0) bằng đệ quy. power(x, 0) = 1."""
    raise NotImplementedError("TODO: cài power bằng đệ quy")
